import pytest

@pytest.fixture(scope="session")
def input_dirpath():
        path = 
        yield path

def output_dirpath():
        path = 
        yield path

def series_keywords():
        dict = []
        yield dict
